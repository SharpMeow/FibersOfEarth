import {glossaryEntries,glossarySources} from './glossary.js';
export const escapeHTML=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function termBody(t,link=id=>'#/glossary/'+id){const e=escapeHTML;return `<p class="definition-lead">${e(t.definition)}</p><section><h2>In detail</h2><p>${e(t.explanation)}</p></section><section><h2>A practical example</h2><p>${e(t.example)}</p></section><section><h2>What to distinguish</h2><p>${e(t.caution)}</p></section><section><h2>Related terms</h2><ul class="related-terms">${t.related.map(name=>{const r=glossaryEntries.find(x=>x.term===name);return `<li><a href="${e(link(r.id))}">${e(name)}</a></li>`;}).join('')}</ul></section><section><h2>Sources & further reading</h2><ul class="source-links">${t.sources.map(id=>{const s=glossarySources[id];return `<li><a href="${e(s.url)}" rel="noopener noreferrer">${e(s.title)}</a></li>`;}).join('')}</ul><p class="editorial-note">Technical references reviewed ${t.reviewed}. Examples are illustrative. Industry organizations and manufacturers describe their own fields; their references are not independent product endorsements.</p></section>`;}
